# ICH AI Detection Pipeline — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# Platform notes:
#   macOS (Apple Silicon): Uses MPS acceleration, standard torch install works
#   Linux (CUDA 12.8):     pip install torch --index-url https://download.pytorch.org/whl/cu128
#   CPU-only:              Works but significantly slower

# ── Deep learning ─────────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
timm>=0.9.12          # MaxViT model (maxvit_base_tf_224.in1k)

# ── Data / numerics ───────────────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0   # AUC, confusion matrix, Youden J threshold
scipy>=1.11.0

# ── DICOM ─────────────────────────────────────────────────────────────────────
pydicom>=2.4.0
pylibjpeg>=2.0.0            # decode JPEG-Lossless CQ500 studies (transfer syntax .70)
pylibjpeg-libjpeg>=2.1.0
pylibjpeg-openjpeg>=2.0.0   # JPEG-2000 (.90) studies
highdicom>=0.22.0          # conformant TID 1500 SR (demo_synthetic/conformant_sr.py)
SimpleITK>=2.3.0           # geometry-preserving series read + GDCM decode (annotation_assist)

# ── PACS ingestion demo ───────────────────────────────────────────────────────
pynetdicom>=2.0.0          # Docker-free C-STORE demo (demo_synthetic/pacs_ingest_demo.py)
dicomweb-client>=0.59.0    # STOW-RS into Orthanc (demo_synthetic/orthanc_stow.py)

# ── Worklist server and image serving ────────────────────────────────────────
flask>=3.0.0
pillow>=10.0.0        # PNG encoding, HU overlay compositing

# ── Model download ────────────────────────────────────────────────────────────
huggingface_hub>=0.20.0   # download_checkpoint.py — fetch checkpoint from HF Hub

# ── Agentic pipeline (optional — only needed for run_demo_agent.py) ──────────
anthropic>=0.25.0     # optional: only required for run_demo_agent.py
                      # Requires ANTHROPIC_API_KEY environment variable.
                      # See README: Agentic Mode — Anthropic API

# ── CQ500 demo study fetcher ──────────────────────────────────────────────────
requests>=2.31.0      # fetch_cq500_demo.py — download CQ500 studies from S3
